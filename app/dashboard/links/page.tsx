import { LinksClient } from "@/components/LinksClient";

export const metadata = {
  title: "Links - Campaign Short Link Manager",
  description: "Create, search, filter, edit, delete, and export branded campaign short links."
};

export default function LinksPage() {
  return <LinksClient />;
}
